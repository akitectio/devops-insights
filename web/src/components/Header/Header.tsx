import React from "react";

const Header: React.FC = () => {
  return (
    <header>
      {/* Nội dung của Header */}
      <nav>{/* Navigation links */}</nav>
    </header>
  );
};

export default Header;
